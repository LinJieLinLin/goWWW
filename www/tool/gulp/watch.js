// var browserSync = require('browser-sync');
module.exports = function(gulp, G, dir) {
    gulp.task('reload', function() {
        // browserSync.reload();
    });
    gulp.task('watch', function() {
        // gulp.watch(dir('src/styles/**/*.scss'), 
        //     G.sync(gulp).sync(['sass', 'reload']));
    });
};
